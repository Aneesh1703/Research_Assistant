# Common dependencies (DB, LLM clients) - placeholders

def get_db():
    """Yield a DB connection (placeholder)."""
    yield None


def get_llm_client():
    """Yield an LLM client (placeholder)."""
    yield None
